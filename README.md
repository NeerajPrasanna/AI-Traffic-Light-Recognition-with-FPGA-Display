# AI-Traffic-Light-Recognition-with-FPGA-Display
A system that detects traffic light colors using a Roboflow AI model via a web interface and sends the result to an FPGA through UART. An Arduino acts as a bridge, and the FPGA displays the detected signal (STOP, SLW, GO, ERR) on 7-segment displays, demonstrating AI integration with embedded hardware.
